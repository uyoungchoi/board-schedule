<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wychoi.app">
 	<select id="boardList" resultType="com.wychoi.app.data.boardData">
 		select
 			writer,
 			writeDate,
 			startDate,
 			endDate,
 			useCalendar,
 			useShare,
 			title,
 			loofQuery,
 			number,
 			content
 		from board
 		where type='board';
 	</select>
 	
 	<insert id="boardAdd" parameterType="com.wychoi.app.data.boardData">
 		insert
 			into board
 			(title, content, useCalendar, useShare, writer, startDate, endDate, writeDate, type)
 			values(
	 			#{title},
	 			#{content},
	 			#{useCalendar},
	 			#{useShare},
	 			#{writer},
	 			#{startDate},
	 			#{endDate},
	 			#{writeDate},
	 			#{type}
 			)
 	</insert>
 	
 	<select id="boardCount" resultType="int">
 		select
 			count(*) as count
 		from board
 		where type='board';
 	</select>
 	
 	<delete id="boardDelete" parameterType="int">
 	 	delete 
 	 		from board 
 	 		where number = #{deleteData};
 	</delete>
 	
 	<select id="detailList"  parameterType="int" resultType="com.wychoi.app.data.boardData">
 		select
 			writer,
 			writeDate,
 			startDate,
 			endDate,
 			useCalendar,
 			useShare,
 			title,
 			loofQuery,
 			number,
 			content
 		from board
 		where number = #{data};
 	</select>
 	
 	<insert id="scheduleAdd" parameterType="com.wychoi.app.data.boardData">
 		insert
 			into board
 			(title, content, useCalendar, useShare, writer, startDate, endDate, writeDate, type)
 			values(
	 			#{title},
	 			#{content},
	 			#{useCalendar},
	 			#{useShare},
	 			#{writer},
	 			#{startDate},
	 			#{endDate},
	 			#{writeDate},
	 			#{type}
 			)
 	</insert>
</mapper> 
 
 